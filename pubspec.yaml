name: reusable_checklists
description: "An offline-first app for creating and reusing checklists."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  hive_ce: ^2.0.0
  hive_ce_flutter: ^2.0.0
  provider: ^6.0.0
  shared_preferences: ^2.2.0
  url_launcher: ^6.2.0
  uuid: ^4.0.0

dev_dependencies:
  build_runner: ^2.0.0
  dependency_validator: ^5.0.5
  flutter_launcher_icons: ^0.14.3
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  hive_ce_generator: ^1.11.1
  integration_test:
    sdk: flutter
  mocktail: ^1.0.0

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#1976D2"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"

flutter:
  uses-material-design: true
